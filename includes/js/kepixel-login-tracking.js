jQuery(document).ready(function ($) {
    _paq = _paq || [];

    // Enhanced ecommerce tracking - trackLogin
    _paq.push(['trackLogin',
        {},                         // user_data - already set in main tracking code if user is logged in
        {}                          // custom_data
    ]);
});
